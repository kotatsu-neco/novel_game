# PATCH_NOTE_v09.md

## 変更内容

1. `SPEC.md` をv0.9仕様へ更新。
2. `DESIGN.md` をv0.9仕様へ更新。
3. `README.md` をv0.9仕様へ更新。
4. ページ分割量・タップ範囲・スクロール禁止をエンジン仕様として明文化。
5. CSSを将来的に `engine.css` / `theme.css` へ分ける方針を整理。
6. `ARCHITECTURE.md` と `AGENTS.md` にも境界ルールを反映。
7. `docs/CSS_SPLIT_PLAN.md` を追加。
8. `manifest` に `engineUiPolicy` を追加。

## 確認済み

- 正本系ファイル更新済み。
- JSON構文OK。
- JS構文OK。
- シーン参照OK。
- CSS参照OK。

## 未確認

- 実ブラウザ表示。
- iPhone Safari実機表示。
